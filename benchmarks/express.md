# Express API Benchmark

Prompt:
"Create a simple Express API with GET /users and POST /users"

Validation Criteria:
- package.json exists in workspace
- package.json contains "express" dependency
- index.js OR app.js OR server.js exists

Result: pass if all criteria met, fail otherwise