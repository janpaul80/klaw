# CLI Tool Benchmark

Prompt:
"Create a CLI tool that logs 'Hello [name]' when run"

Validation Criteria:
- package.json exists in workspace
- package.json contains "bin" entry
- index.js OR cli.js OR main.js exists

Result: pass if all criteria met, fail otherwise